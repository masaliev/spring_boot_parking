version: '3'

services:
  postgres:
    #restart: always
    image: postgres:12.3
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data/
    environment:
      - POSTGRES_PASSWORD=postgres123

volumes:
  pgdata: